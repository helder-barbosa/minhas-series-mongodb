<%- include('../partials/header') %>


  <section id="about">
    <div class="container">
      <div class="row">
        <div class="col-lg-8 mx-auto">
          <h2>Series</h2>
          <p class="lead">Todas Séries Cadastradas <a href="/series/nova" class="btn btn-primary">Nova Serie</a></p>
          <table class="table table-hover table-striped">
            <% series.forEach( serie=> { %>
              <tr>
                <td>
                  <%= serie.name %>
                </td>
                <td>
                  <%= serie.status %>
                </td>
              </tr>
              <% }) %>
          </table>
        </div>
      </div>
    </div>
  </section>


  <%- include('../partials/footer') %>